{% extends 'layout.twig' %} {% block body %}
<main class="home">
  <h1 class="home__title">{{ title }}</h1>
  <p class="home__body">{{ 'Hit the button to rate me out of 10' }}</p>

  <figure class="home__image">
    <img
      srcset="/images/spake-large.jpg 800w, /images/spake-small.jpg 400w"
      src="/images/spake-small.jpg"
    />
  </figure>

  {% if error %}
  <p class="home__body">Error getting dynamic content</p>
  {% else %}
  <a href="/home/comment/create" class="button button--full button--mb">
    <span>
      {{ 'rate my mate' }}
    </span>
  </a>
  <p class="home__body">
    Jake has been rated by {{ data.comment_count }} people so far!
  </p>
</main>

{% endif %} {% endblock %}
